/**
 * @file Enumeration representing the different account types a user can have.
 */
enum AccountType {
    Personal = "PERSONAL",
    Academic = "ACADEMIC",
    Professional = "PROFESSIONAL"
};

export default AccountType;